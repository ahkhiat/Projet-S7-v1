{% extends 'base.html.twig' %}

{% block title %}Users list{% endblock %}

{% block body %}

<h1>Users list</h1>

    <div class="w-75 mx-auto">            
    <a href="/form"><button type="button" class="btn btn-primary btn">Add new user</button></a>
    </div> 

<table class="table w-75 mx-auto table-striped table-bordered table-hover">

    <tr>
    <TH>Nom </TH> 
    <TH>Prénom </TH>
    <TH>Email</TH>
    <TH>Action</TH>
    </tr>

    {% for u in users %}

    <tr>

    {# On peut mettre .nom ou getNom() #}
    <td> {{ u.nom }}  </td>  
    <td> {{ u.prenom }} </td>
    <td> {{ u.email }} </td>

    <td>
        <div class="d-flex ">
        {# <a href="{{ path('person_show', {'id': u.id}) }}"><button type="button" class="btn btn-primary btn-sm mx-1"><i class="bi bi-eye-fill"></i></button></a> #}
        <a href="{{ path('app_user_edit', {'id': u.id}) }}"><button type="button" class="btn btn-warning btn-sm mx-1"><i class="bi bi-pen-fill"></i></button></a>
        {# <a href="{{ path('person_delete', {'id': u.id}) }}"><button type="button" class="btn btn-danger btn-sm mx-1"><i class="bi bi-trash-fill"></i></button></a> #}
        </div>
    </td>

    </tr>

{% endfor %}

</table>



{% endblock %}
