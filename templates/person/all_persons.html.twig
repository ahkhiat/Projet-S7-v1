{% extends 'base.html.twig' %}

{% block title %}Liste des personnes{% endblock %}

{% block body %}


<h1>Liste des personnes</h1>

    <div class="w-75 mx-auto">            
    <a href="/persons/add_person"><button type="button" class="btn btn-primary btn">Add new user</button></a>
    </div> 

<table class="table w-75 mx-auto table-striped table-bordered table-hover">

<tr>
<TH>Nom </TH> <TH>Prénom </TH>
<TH> Ville</TH>
<th>Age</th>
<th>Date de naissance</th>
<th>Action</th>
</tr>
{% for person in persons %}

<tr>

{# On peut mettre .nom ou getNom() #}
<td> {{ person.nom }}  </td>  
<td> {{ person.prenom }} </td>
<td> {{ person.ville }} </td>
<td> {{ person.age }} </td>
<td> {{ person.getDateNaissance()|date('Y-m-d') }} </td>

<td>
<div class="d-flex ">
<a href="{{ path('person_show', {'id': person.id}) }}"><button type="button" class="btn btn-primary btn-sm mx-1"><i class="bi bi-eye-fill"></i></button></a>

<a href="{{ path('person_edit', {'id': person.id}) }}"><button type="button" class="btn btn-warning btn-sm mx-1"><i class="bi bi-pen-fill"></i></button></a>

<a href="{{ path('person_delete', {'id': person.id}) }}"><button type="button" class="btn btn-danger btn-sm mx-1"><i class="bi bi-trash-fill"></i></button></a>

</div>
</td>

</tr>

{% endfor %}

</table>

{# {{ path('person_show', {'id' : person.id})}} #}

{% endblock %}