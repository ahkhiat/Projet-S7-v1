{% extends 'base.html.twig' %}

{% block title %}Person Show{% endblock %}

{% block body %}

<h1>Fiche personnelle</h1>

<table class="table w-75 mx-auto table-bordered">
<tr>

<TH>Nom </TH>
<TH>Prénom </TH>
<TH> Ville</TH>
<TH> Age</TH>

</tr>
{# {% for person in persons %} #}

<tr>
<td> {{ person.getNom() }}  </td>
<td> {{ person.getPrenom() }} </td>
<td> {{ person.getVille() }} </td>
<td> {{ person.getAge() }} </td>
<td> {{ person.getDateNaissance()|date('Y-m-d') }} </td>
{# <td><a href="{{ path('person_show', {'id': person.id}) }}">show</a></td> #}

</tr>

{# {% endfor %} #}


</table>

<div class="text-center">
<a href="{{ path('person_edit', {'id': person.id}) }}"><button type="button" class="btn btn-primary ">Edit</button></a>

<a href="/persons"><button type="button" class="btn btn-primary ">All users</button></a>
</div>

{% endblock %}
